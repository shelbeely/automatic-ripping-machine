<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ARM - History</title>
  <link href="https://cdn.jsdelivr.net/npm/beercss@3.7.14/dist/cdn/beer.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons+Outlined" rel="stylesheet">
  <link rel="stylesheet" href="/static/css/style.css">
</head>
<body class="dark">
  <%- include('nav') %>
  <div class="page">
    <h5>Job History</h5>
    <% if (typeof jobs !== 'undefined' && jobs.length > 0) { %>
      <table class="stripes">
        <thead>
          <tr><th>ID</th><th>Title</th><th>Type</th><th>Status</th><th>Started</th><th>Finished</th><th>Actions</th></tr>
        </thead>
        <tbody>
          <% jobs.forEach(function(job) { %>
            <tr>
              <td><a href="/jobdetail?job_id=<%= job.job_id %>"><%= job.job_id %></a></td>
              <td><%= job.title || 'Unknown' %></td>
              <td><%= job.disctype %></td>
              <td><span class="chip small <%= job.status === 'success' ? 'tertiary' : 'error' %>"><%= job.status %></span></td>
              <td><%= new Date(job.start_time).toLocaleString() %></td>
              <td><%= job.stop_time ? new Date(job.stop_time).toLocaleString() : 'N/A' %></td>
              <td><a href="/jobdetail?job_id=<%= job.job_id %>" class="button small border">Details</a></td>
            </tr>
          <% }); %>
        </tbody>
      </table>
    <% } else { %>
      <div class="primary-container round padding">
        <i>info</i>
        <span>No completed jobs yet.</span>
      </div>
    <% } %>
  </div>
  <script type="module" src="https://cdn.jsdelivr.net/npm/beercss@3.7.14/dist/cdn/beer.min.js"></script>
  <script type="module" src="https://cdn.jsdelivr.net/npm/material-dynamic-colors@1.1.4/dist/cdn/material-dynamic-colors.min.js"></script>
</body>
</html>
